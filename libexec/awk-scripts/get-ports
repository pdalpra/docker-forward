BEGIN {
  protocol="";
  port="";
}
{
  if($0 ~ /^NIC 1 Rule/) {
    # match the protocol
    match($0, /(tcp|udp)/, arr)
    protocol=arr[0]
    # match the port
    match($0, /guest port = ([[:digit:]]+)/, arr)
    port=arr[1]
    print protocol"/"port
  }
}
